<div class="container mx-auto px-4 py-8 max-w-2xl">
  <h1 class="text-2xl font-bold mb-6">投稿を編集する</h1>

  <%= render 'form', post: @post %>

  <div class="mt-4">
    <%= link_to "詳細に戻る", posts_path(@post), class: "text-gray-500 hover:underline" %>
  </div>
</div>